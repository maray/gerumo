10 abril 2026

camera.py:
Se agregsron los datos de diccionario _images atributes para la imagen y peak time en DL1.
load_camera: agregada compatibilidad con DL1.

constants.py:
Agregados los TELESCOPES_ALIAS para DL1

dataset.py:
agregado DL1 en diccionarios relevantes.
extract_data: agregada la variable de entrada n_file que indica el número del archivo que se está leyendo al cargar datos de una carpeta.
Agregada compatibilidad con DL1. El campo event_unique_id es reemplazado por obs_id _ event_id _ n_file en el caso de DL1.
El campo observation_indice es suplantado por el valor str(event["obs_id"]) + "_" + str(event["event_id"]) + "_" + str(tel_id) + "_" + str(n_file).
agregate_dataset y filter_dataset ahora toman la variable de entrada version y son compatibles con DL1. agregate_dataset transforma los ángulos en grados a radianes.

Agregados archivos de posición de pixeles para LST y MST 

Preprocesing debug.ipynb:
Cuaderno de debug de funciones de prerocesado. Incluye el método para crear los archivos de pixels_positions que hay hasta la fecha.

prod3 explorer.ipynb:
Cuaderno para explorar un archivo prod3 de referencia.

Test_Sven_preprocesing.ipyng:
Cuaderno de pruebas y exploración de archivos prod5.

22 abril 2026

camera.py:
Agregada compatibilidad para DL1 en cameras_to_images.

constants.py:
Agregado el dict TELESCOPES_INVERSE_ALIAS.
Agregada "MST_NectarCam" a la lista de telescopios, camaras, aliases, tamaños de cámara y formas de entrada.
Se agregaron los archivos de posición de pixeles al diccionario PIXELS_POSITION con compatibilidad para DL1 y NectarCam.
Ahora si no se encuentra un archivo, se enviará un aviso en vez de no cargar ningún archivo de posición de pixeles.

dataset.py:
Cambios en extract_data hacen que al generar el archivo csv de datos se guarde el valor de los ángulos al y az en radianes de forma correcta.
Para compensar esto, se quito esta función de la función agregate_dataset.

misc.py:
extract_pixel_positions, la función que genera los archivos de posición de pixeles de los telescopios fue modificada para poder extraer información de prod5.
Se agregó la función para generar directamente los archivos de "time.npy" y "time-shift.npy" en vez de tener que copiar los "simple.npy" y "simple-shift.npy"
La función get_alphas fue actualizada con los valores de "LST_LSTCam" y "MST_NectarCam" para "DL1". Para esto requiere del parametro de entrada extra version.

preprocessing.py:
Agregado código para poder cargar los escalares de DL1.
Reparado bug que no dividia la matriz de carga por el escalar de carga de forma correcta.

umona.py:
Agregada opción de tamaño de kernel para "MST_NectarCam". Mismo tamaño que "LST_LSTCam".

preprocesing_debug.ipynb:
Agregada formulas para obtener los alfas.
Trabajo y debug de pipes.

pipes_debug.ipynb:
Notebook para debugear pipes y otras funciones de las cámaras.

umona_LST.json y umona_MST.json:
archivos json con la configuración para entrenar los telescopios LST y MST, respectivamente.


25 abril 2026

generator.py:
cambiado el valor de batch_telescope_features y event_telescope_features en el asembler para evitar el error ""X does not have valid feature names, but"

29 abril 2026

camera.py:
Se cambió load_camera_geometry para generar la geometria de cero en vez de utilizar un template predefinido. Se agregó un diccionario con los tamaños de pixeles para este propósito.
Se arregló load_cameras para ser más rápido tomando directamente los indices de datos de imágenes del csv de datos en vez de buscarlos dentro del archivo hdf5. Mejora de 20 veces de velocidad.

dataset.py:
Se agregó la colimna "image_index" al dataset de telescopios generado por extract_data. Se utiliza para obtener el número de fila directamente de un evento en la tabla de imágenes del archivo hdf5 correspondiente. La función build_table_index se utilica para generar un índice respectivo al obs_id, event_id y tel_id.
En agregate_dataset se agregó un .iloc para compatibilizar mejor con python 3.10.

assembler.py:
Se agregó el atributo y la entrada necesarios para poder procesar telescopios de tipo MST_NectarCam. Esta lógica se extendió a los archivos bmo.py, cnn_det.py y umonna.py


